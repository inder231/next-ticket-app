import CreateForm from "./CreateForm";

export default function CreateTicket() {
  return (
    <main>
      <h2 className="text-priamary text-center">Add a new Ticket</h2>
      <CreateForm />
    </main>
  );
}
